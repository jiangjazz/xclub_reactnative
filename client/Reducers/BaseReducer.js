const INITIAL_STATE = {
  // url
  baseUrl: 'http://xclub.pre.transsion.net'
};

export default baseReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
